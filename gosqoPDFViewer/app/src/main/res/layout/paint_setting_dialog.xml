<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="350dp"
    android:layout_height="500dp"
    android:background="#E5F3FB"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:context="viewer_pro.viewer._Paint_Settings_popup">

    <!-- Title, Close Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#0A71A5"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="290dp"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:padding="10dp"
            android:id="@+id/tv_popup_title"
            android:text="Paint Color Select"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btn_close_colorpopup"
            android:layout_width="30dp"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:src="@drawable/close_app" />


    </LinearLayout>

    <!-- MainColorPickSelection -->
    <ListView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="7"
        android:id="@+id/list_color_select"/>


    <!-- SizeAdjustment Selection -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#0A71A5"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- 컨트롤 버튼 이름 Thumbs -->
        <SeekBar
            android:id="@+id/skbr_paint_adjust"
            android:layout_width="280dp"
            android:layout_height="match_parent"
            android:max="29"
            android:progress="0" />


        <TextView
            android:id="@+id/tv_paintsize"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:layout_marginRight="5dp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>

